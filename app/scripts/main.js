'use strict';
$(document).ready(function(){

  // Injecting SVG-sprite

  /*
  var svgPath = 'images/svg-defs.svg';
  $.get(svgPath, function(data) {
    var $icons = $('<div class="icons"></div>').html(new XMLSerializer().serializeToString(data.documentElement));
    $('body').prepend($icons);
  });
  */

});
